import Split from "react-split";
import dynamic from "next/dynamic";

import MarkdownPanel from "../components/MarkdownPanel";
const TerminalPanel = dynamic(() => import("../components/TerminalPanel"), {
  ssr: false,
});

export default function Home() {
  return (
    <div className="h-screen w-screen">
      <Split
        className="split"        // ⬅️ ini wajib cocok sama CSS .split
        direction="horizontal"   // bikin kiri-kanan
        sizes={[50, 50]}         // masing-masing 50%
        minSize={200}
        gutterSize={8}
      >
        <div className="h-full overflow-auto bg-white">
          <MarkdownPanel />
        </div>
        <div className="h-full overflow-auto bg-black text-white">
          <TerminalPanel />
        </div>
      </Split>
    </div>
  );
}
